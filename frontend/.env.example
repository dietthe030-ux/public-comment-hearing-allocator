# Public Comment Hearing Allocator - Environment Configuration
# GenLayer Studionet Contract Address (0x followed by 40 hexadecimal characters)
# Leave empty until contract deployment is completed.
# If unset or invalid, the frontend runs in honest read-only "Deployment pending" mode.
VITE_CONTRACT_ADDRESS=
